import { IonFooter, IonTitle, IonToolbar } from "@ionic/react"


export const NavBar = () =>{
    return(
        <IonFooter>
            <IonToolbar>
                <IonTitle>Footer</IonTitle>
            </IonToolbar>
        </IonFooter>
    )
}